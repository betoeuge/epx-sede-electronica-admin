import { SetupWizard } from "./_components/SetupWizard";

export const metadata = { title: "Instalación — Sede Electrónica Admin" };

export default function SetupPage() {
  return <SetupWizard />;
}
